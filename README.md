# FileExplorer Date Grouping DataGrid
Example DataGrid with grouping and sorting like File Explorer in Windows

The main take away is that the groups get reversed if sorting by date in descending order.
